diff --git b/worker/comment-submission-routes.ts a/worker/comment-submission-routes.ts
index d30dc7a..02941e1 100644
--- b/worker/comment-submission-routes.ts
+++ a/worker/comment-submission-routes.ts
@@ -829,8 +829,8 @@ routes.post("/comment-submissions/:submissionId/finalize", async (c) => {
     }));
     for (const target of occurrenceTargets) statements.push(c.env.DB.prepare(
       `INSERT INTO run_step_comments
-       (id, run_step_id, scope, operation_group_id, submission_id, actor_email, created_at)
-       SELECT ?, rs.id, ?, ?, ?, ?, ?
+       (id, run_step_id, scope, operation_group_id, body, submission_id, actor_email, created_at)
+       SELECT ?, rs.id, ?, ?, ?, ?, ?, ?
        FROM comment_submission_targets cst
        JOIN run_steps rs ON rs.id = cst.run_step_id AND rs.run_id = cst.run_id
        JOIN runs r ON r.id = cst.run_id AND r.sample_id = cst.sample_id
@@ -843,6 +843,7 @@ routes.post("/comment-submissions/:submissionId/finalize", async (c) => {
       target.occurrenceId,
       submission.scope,
       operationGroupId,
+      submission.body,
       submissionId,
       userEmail,
       now,
diff --git b/worker/evidence/legacy-routes.ts a/worker/evidence/legacy-routes.ts
index ded511d..390acfe 100644
--- b/worker/evidence/legacy-routes.ts
+++ a/worker/evidence/legacy-routes.ts
@@ -80,8 +80,8 @@ routes.post("/run-step-comments", async (c) => {
        WHERE rs.updated_at = q.expected_updated_at
      )
      INSERT INTO run_step_comments
-       (id, run_step_id, scope, operation_group_id, body, legacy_body, asset_id, actor_email, created_at)
-     SELECT valid.comment_id, valid.step_id, ?, ?, ?, ?, ?, ?, ?
+       (id, run_step_id, scope, operation_group_id, body, asset_id, actor_email, created_at)
+     SELECT valid.comment_id, valid.step_id, ?, ?, ?, ?, ?, ?
      FROM valid
      WHERE (SELECT COUNT(*) FROM valid) = ?
      RETURNING id`,
@@ -90,7 +90,6 @@ routes.post("/run-step-comments", async (c) => {
     input.scope,
     operationGroupId,
     body,
-    body,
     commentAsset?.id ?? null,
     userEmail,
     now,
@@ -208,7 +207,7 @@ routes.delete("/run-step-comments/:id/asset", async (c) => {
   const targets = removeCommonGroup
     ? await c.env.DB.prepare(
       `SELECT rsc.id, rsc.run_step_id,
-              CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body
+              CASE WHEN rsc.submission_id IS NULL THEN rsc.body
                    ELSE (SELECT cs.body FROM comment_submissions cs WHERE cs.id = rsc.submission_id) END AS body, r.sample_id, rs.updated_at, s.updated_at AS sample_updated_at
        FROM run_step_comments rsc JOIN run_steps rs ON rs.id = rsc.run_step_id
        JOIN runs r ON r.id = rs.run_id JOIN samples s ON s.id = r.sample_id
@@ -226,7 +225,7 @@ routes.delete("/run-step-comments/:id/asset", async (c) => {
     ).bind(comment.operation_group_id).all<{ id: string; run_step_id: string; body: string; sample_id: string; updated_at: string; sample_updated_at: string }>()
     : await c.env.DB.prepare(
       `SELECT rsc.id, rsc.run_step_id,
-              CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body
+              CASE WHEN rsc.submission_id IS NULL THEN rsc.body
                    ELSE (SELECT cs.body FROM comment_submissions cs WHERE cs.id = rsc.submission_id) END AS body, r.sample_id, rs.updated_at, s.updated_at AS sample_updated_at
        FROM run_step_comments rsc JOIN run_steps rs ON rs.id = rsc.run_step_id
        JOIN runs r ON r.id = rs.run_id JOIN samples s ON s.id = r.sample_id
@@ -597,7 +596,7 @@ routes.delete("/run-step-comments/:id", async (c) => {
   const targets = removeCommonGroup
     ? await c.env.DB.prepare(
       `SELECT rsc.id, rsc.run_step_id,
-              CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body
+              CASE WHEN rsc.submission_id IS NULL THEN rsc.body
                    ELSE (SELECT cs.body FROM comment_submissions cs WHERE cs.id = rsc.submission_id) END AS body, rsc.asset_id, r.sample_id, rs.updated_at, s.updated_at AS sample_updated_at
        FROM run_step_comments rsc
        JOIN run_steps rs ON rs.id = rsc.run_step_id
@@ -617,7 +616,7 @@ routes.delete("/run-step-comments/:id", async (c) => {
     ).bind(comment.operation_group_id).all<{ id: string; run_step_id: string; body: string; asset_id: string | null; sample_id: string; updated_at: string; sample_updated_at: string }>()
     : await c.env.DB.prepare(
       `SELECT rsc.id, rsc.run_step_id,
-              CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body
+              CASE WHEN rsc.submission_id IS NULL THEN rsc.body
                    ELSE (SELECT cs.body FROM comment_submissions cs WHERE cs.id = rsc.submission_id) END AS body, rsc.asset_id, r.sample_id, rs.updated_at, s.updated_at AS sample_updated_at
        FROM run_step_comments rsc
        JOIN run_steps rs ON rs.id = rsc.run_step_id
@@ -804,7 +803,7 @@ routes.post("/run-step-comments/:id/restore", async (c) => {
   const targets = restoreCommonGroup
     ? await c.env.DB.prepare(
       `SELECT rsc.id, rsc.run_step_id,
-              CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body
+              CASE WHEN rsc.submission_id IS NULL THEN rsc.body
                    ELSE (SELECT cs.body FROM comment_submissions cs WHERE cs.id = rsc.submission_id) END AS body, r.sample_id, a.r2_key,
               rs.updated_at, s.updated_at AS sample_updated_at
        FROM run_step_comments rsc
@@ -829,7 +828,7 @@ routes.post("/run-step-comments/:id/restore", async (c) => {
     }>()
     : await c.env.DB.prepare(
       `SELECT rsc.id, rsc.run_step_id,
-              CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body
+              CASE WHEN rsc.submission_id IS NULL THEN rsc.body
                    ELSE (SELECT cs.body FROM comment_submissions cs WHERE cs.id = rsc.submission_id) END AS body, r.sample_id, a.r2_key,
               rs.updated_at, s.updated_at AS sample_updated_at
        FROM run_step_comments rsc
diff --git b/worker/references/adapters.ts a/worker/references/adapters.ts
index 0af700c..fbc16e2 100644
--- b/worker/references/adapters.ts
+++ a/worker/references/adapters.ts
@@ -313,7 +313,7 @@ const commentAdapter: ReferenceAdapter = async (db, ids) => {
 const commentOccurrenceAdapter: ReferenceAdapter = async (db, ids) => {
   const rows = await allRows(db, `
     SELECT rsc.id, rsc.scope, rsc.submission_id, cs.id AS canonical_submission_id,
-           CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body ELSE cs.body END AS body,
+           CASE WHEN rsc.submission_id IS NULL THEN rsc.body ELSE cs.body END AS body,
            CASE WHEN rsc.submission_id IS NULL THEN 'legacy' ELSE cs.status END AS comment_state,
            COALESCE(rsc.updated_at, rsc.created_at) AS updated_at,
            COALESCE(rsc.deleted_at, cs.deleted_at) AS deleted_at,
diff --git b/worker/references/search.ts a/worker/references/search.ts
index 3a81dde..da9df29 100644
--- b/worker/references/search.ts
+++ a/worker/references/search.ts
@@ -563,7 +563,7 @@ export const REFERENCE_SEARCH_ADAPTERS = {
     `,
     idSql: "rsc.id",
     primarySqls: [],
-    contentSqls: ["CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body ELSE '' END"],
+    contentSqls: ["CASE WHEN rsc.submission_id IS NULL THEN rsc.body ELSE '' END"],
     metadataSqls: [
       "CASE WHEN rsc.submission_id IS NULL THEN rsc.scope ELSE '' END",
       "CASE WHEN rsc.submission_id IS NULL THEN COALESCE(rs.title, sd.name) ELSE '' END",
diff --git b/worker/samples/routes.ts a/worker/samples/routes.ts
index 9fcd54a..1a1dbfb 100644
--- b/worker/samples/routes.ts
+++ a/worker/samples/routes.ts
@@ -664,7 +664,7 @@ routes.get("/samples/:id", async (c) => {
     ).bind(id).all<{ run_id: string; r2_key: string }>(),
     c.env.DB.prepare(
       `SELECT rsc.id, rsc.run_step_id, rsc.scope, rsc.operation_group_id,
-              CASE WHEN rsc.submission_id IS NULL THEN rsc.legacy_body ELSE cs.body END AS body,
+              CASE WHEN rsc.submission_id IS NULL THEN rsc.body ELSE cs.body END AS body,
               ca.r2_key AS asset_key, rsc.submission_id, rsc.actor_email, rsc.created_at
        FROM run_step_comments rsc
        JOIN run_steps rs ON rs.id = rsc.run_step_id
